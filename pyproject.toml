[build-system]
requires = ["setuptools>=68", "versioningit~=3.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sabueso"
description = "Biomolecular knowledge integration into cards and decks traced to source assertions."
readme = "README.md"
requires-python = ">=3.11,<3.15"
authors = [{ name = "UIBCDF Lab", email = "uibcdf@gmail.com" }]
license = { text = "MIT" }
dynamic = ["version"]
# Runtime dependencies, identical to the conda recipe's `requirements: run`. They are
# published on the uibcdf conda channel, not on PyPI: install them with conda and use
# `pip install --no-deps` for Sabueso itself (MOLI distribution policy, #34).
dependencies = [
  "smonitor>=0.16.0",
  "pyunitwizard>=0.27.0",
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]

[project.optional-dependencies]
test = [
  "pytest>=8",
  # Reporting for agents and CI logs (MOLI developer-tools policy, uibcdf/sabueso#31).
  # Development tooling, not a runtime dependency; pinned to the published release.
  "pytest-receptor==1.1.0",
  "pyyaml>=6",
  "ruff==0.16.5",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["sabueso*"]

# Non-Python files the package reads at runtime (uibcdf/sabueso#35). Every such file must
# be listed here; tests/core/test_package_data_offline.py enforces it.
[tool.setuptools.package-data]
sabueso = ["resolver/*.json"]

[tool.pytest.ini_options]
markers = [
  "online: tests that require network access",
]

[tool.ruff]
target-version = "py311"
extend-exclude = ["MOLI_GUIDE.md"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.versioningit]
default-version = "0.0.0+unknown"

[tool.versioningit.format]
distance = "{base_version}+{distance}.{vcs}{rev}"
dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"
distance-dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"

[tool.versioningit.vcs]
method = "git"
match = ["*"]
default-tag = "0.0.0"

[tool.versioningit.tag2version]
regex = "^(?P<version>(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*))$"
require-match = true

[tool.versioningit.write]
file = "sabueso/_version.py"
