version = "0.1.0"
route = "staged"
reason = "First public Sabueso release (legacy 0.0.1-0.2.0 packages were withdrawn from the channel); clean installed-package gates with the public SMonitor, PyUnitWizard and DepDigest builds must pass before public visibility."
decision_by = "Diego Prada (Sabueso maintainer), 2026-09-24"
required_workflows = [
  ".github/workflows/ci.yml",
  ".github/workflows/moli-governance.yml",
]
