version = "0.3.0"
route = "staged"
reason = "First publication of card schema 0.3.2; new modules that persist data (the SQLite knowledge store with content-addressed snapshots, sabueso/core/knowledge_store.py and snapshot.py); a new source (AlphaFold DB); and a change of curated SourceAssertion ids with an automatic migration of curation stores (#62). Clean installed-package gates, Windows included, must pass before public visibility."
decision_by = "Diego Prada (Sabueso maintainer), 2026-09-25"
required_workflows = [
  ".github/workflows/ci.yml",
  ".github/workflows/moli-governance.yml",
]
