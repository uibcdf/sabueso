version = "0.1.1"
route = "staged"
reason = "Fixes 0.1.0, whose package lacked sabueso/resolver/selection_rules.json (uibcdf/sabueso#35); clean installed-package gates, now building a card from a mapping, must pass before public visibility."
decision_by = "Diego Prada (Sabueso maintainer), 2026-09-24"
required_workflows = [
  ".github/workflows/ci.yml",
  ".github/workflows/moli-governance.yml",
]
