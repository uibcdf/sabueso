version = "0.2.0"
route = "staged"
reason = "New direct runtime dependency (depdigest), new packaged data (sabueso/resolver/enrichment_profiles.json), an optional dependency checked at call time (pandas), and the first publication of card schema 0.3.1; clean installed-package gates must pass before public visibility."
decision_by = "Diego Prada (Sabueso maintainer), 2026-09-24"
required_workflows = [
  ".github/workflows/ci.yml",
  ".github/workflows/moli-governance.yml",
]
